services:
  cloudflared:
    image: cloudflare/cloudflared:latest
    container_name: whoamai-cloudflared
    restart: unless-stopped
    depends_on:
      - telegram-bot
    command: tunnel --no-autoupdate --url http://telegram-bot:8000
